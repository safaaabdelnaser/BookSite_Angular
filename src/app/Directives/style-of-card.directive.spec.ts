import { StyleOfCardDirective } from './style-of-card.directive';

describe('StyleOfCardDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleOfCardDirective();
    expect(directive).toBeTruthy();
  });
});
