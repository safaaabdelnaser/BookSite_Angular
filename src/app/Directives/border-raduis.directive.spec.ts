import { BorderRaduisDirective } from './border-raduis.directive';

describe('BorderRaduisDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderRaduisDirective();
    expect(directive).toBeTruthy();
  });
});
