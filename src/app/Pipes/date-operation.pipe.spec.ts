import { DateOperationPipe } from './date-operation.pipe';

describe('DateOperationPipe', () => {
  it('create an instance', () => {
    const pipe = new DateOperationPipe();
    expect(pipe).toBeTruthy();
  });
});
