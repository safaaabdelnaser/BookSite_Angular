import { CerditCardPipe } from './cerdit-card.pipe';

describe('CerditCardPipe', () => {
  it('create an instance', () => {
    const pipe = new CerditCardPipe();
    expect(pipe).toBeTruthy();
  });
});
